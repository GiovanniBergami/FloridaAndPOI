package connectors;

public class clientConnector {
    //se voglio farlo vado su swagger e rifaccio il progetto definendo le API e usando spring per il server, e rimetto le
    // funzioni definite qua li, e python flask o java per il client, e ci metto una cli
}
